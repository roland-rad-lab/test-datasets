
profiles {
	charliecloud {
		charliecloud {
			runOptions = "--no-passwd --bind /media/rad/SSD1/MoCaSeq_ref:/var/pipeline/ref --bind ${projectDir}/repository:/var/pipeline/repository"
		}

		process {
			container = '/media/rad/SSD1/images-live/atacseq_analysis'
		}
	}
}

params.genomes_base = "/var/pipeline/ref"
params.script_base = "/var/pipeline/repository"
